#pragma once

#define FAST_IO                       \
    ios_base::sync_with_stdio(false); \
    cin.tie(0);                       \
    cout.tie(0);                      \
    clog.tie(0);                      \
    cerr.tie(0);

#define ll long long int
#define ld long double